rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    
    // Anyone on the internet can upload, read, or delete images
    match /projects/{projectId}/{allPaths=**} {
      allow read, write: if true;
    }
  }
}
